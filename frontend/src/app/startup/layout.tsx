import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Startup Dashboard — HopeFusion Africa',
  description: 'Manage your startup profile, track investor matches, discover grants, and grow your ecosystem connections on HopeFusion Africa.',
};

export { default } from './page';
